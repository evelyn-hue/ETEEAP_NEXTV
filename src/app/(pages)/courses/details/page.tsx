"use client";

import { Details } from "@/components/courses";
import {Header, Footer} from "@/components/landpage";

export default function DetailsPage() {

    return (
        <main>
        <Header />
        <Details/>
        <Footer />
        </main>
    );

}